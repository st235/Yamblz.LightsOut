package xyz.javablog.logic;

import logic.models.MatrixEngine;
import models.Field;
import models.Matrix;
import models.Point;

public class GameMatrixEngine implements MatrixEngine {

    @Override
    public Field pushForward(Point point, Matrix matrix) {

        return null;
    }
}
